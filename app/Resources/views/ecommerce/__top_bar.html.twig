<div class="container">
    <div class="row">
        <div class="col d-flex flex-row">
            <div class="top_bar_contact_item">
                <div class="top_bar_icon">
                    Hello
                    {% if app.user %}
                        {{ app.user.username }}
                    {% else %}
                        stranger
                    {% endif %}
                    !
                </div>
            </div>
            <div class="top_bar_content ml-auto">
                <div class="top_bar_user">
                    <div class="user_icon"><img
                                src="{{ asset('img/user.svg') }}"
                                alt=""></div>
                    <div><a href="{{ path('fos_user_registration_register') }}">Register</a></div>
                    {% if app.user %}
                        <div><a href="{{ path('fos_user_security_logout') }}">Sign out</a></div>
                    {% else %}
                        <div><a href="{{ path('fos_user_security_login') }}">Sign in</a></div>
                    {% endif %}
                    <div><a href="{{ path('sonata_admin_dashboard') }}">Dashboard</a></div>
                    {#                    <div><a href="{{ path('fos_user_security_login') }}">Dashboard</a></div>#}
                </div>
            </div>
        </div>
    </div>
</div>